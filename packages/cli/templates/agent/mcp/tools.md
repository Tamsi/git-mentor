# MCP tools reference (template stub)

The live reference is **generated on every `gitmentor init`** into:

`~/.config/git-mentor/mcp/tools.md`

Source: `packages/cli/src/mcp-tools-reference.ts` (built from `GITHUB_MCP_TOOL_DEFINITIONS` + builtin tool list).

Do not edit this template for tool additions — update the GitHub MCP handlers / shipped tools constants instead, then run `gitmentor init`.
